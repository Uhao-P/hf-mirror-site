MIRROR_HOST=localhost
# 如果是公网域名且使用 Cloudflare DNS，请设置 CF_TOKEN。本地使用 localhost 时通常可忽略。
CF_TOKEN=xxxx
API_KEY=""
# 代理配置 (HTTP/HTTPS 代理)
OUTBOUND_PROXY=http://127.0.0.1:6666
# 如果需要 SOCKS5，可以设置为 socks5://127.0.0.1:6667 (注意：Caddy 标准 HTTP transport 可能不支持 SOCKS5 代理配置，建议优先使用 HTTP 代理)

# LFS 上游配置 (默认为官方地址)
# 如果启用本地自动缓存脚本 (scripts/lfs_cache_proxy.py)，请将其修改为 http://127.0.0.1:50001
LFS_UPSTREAM=http://127.0.0.1:50001
# LFS_UPSTREAM=https://cdn-lfs.huggingface.co
LFS_UPSTREAM_US=http://127.0.0.1:50001
# LFS_UPSTREAM_US=https://cdn-lfs-us-1.huggingface.co
